TODO:

docker definitions:
if env.developent = localhost
if env.production = dockerIP

hostname of the server
port

FRONT:
docker needs to know port and ip

two seperate

change coordinates based on container size

לעבוד עם קואורדינטטות של אחוזים ולא מספרים
לבדוק איך עושים DEPLOYEMNT עם NEXTJS
